import { AddressSplitPipe } from './address-split.pipe';

describe('AddressSplitPipe', () => {
  it('create an instance', () => {
    const pipe = new AddressSplitPipe();
    expect(pipe).toBeTruthy();
  });
});
